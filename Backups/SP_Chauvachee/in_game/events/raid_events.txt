namespace = raid_events

raid_events.1 = {
    type = country_event
    title = raid_events.1.t
    desc  = raid_events.1.d

    is_triggered_only = yes
    hidden            = no
    interface_lock    = no   # does not pause the game

    # No trigger block – the scripted action decides whether to fire it.

    option = {
        name = raid_events.1.a
        # Just acknowledge the raid
    }

    option = {
        name = raid_events.1.b   # "Disable raid notifications"
        # Mark this country so future raids won't pop this event
        set_variable = { name = raid_notifications_disabled value = 1 }
    }
}
