######################
# Xebec - fast, shallow draft, lateen rigged frigates. Cheaper, though less heavily armed then european frigates.
######################

# Early Xebec
ens_unlock_xebec = {
	age = age_3_discovery
	requires = port_cost_distance_from_capital_advance

	potential = {
		original_capital ?= {
			OR = {
				region = region:egypt_region
				region = region:maghreb_region
			}
		}
	}

	unlock_unit = n_ens_xebec


	ai_preference_tags = {
		exploration #
	}

	ai_weight = {
		add = "ai_unlock_unit_score(n_ens_xebec)"
	}
}

# Late Xebec - this advance is still not correctly showing an unlock... why? the one above does just fine... is it because the age 4 light ship isn't available for Maghreb? Test by switching the Age 4 xebec to use age 3 light ship, or Age 3 to use Age 4 light ship

ens_unlock_late_xebec = {
	age = age_4_reformation
	requires = merchant_power_from_maritime_reformation_advance

	potential = {
		original_capital ?= {
			OR = {
				region = region:egypt_region
				region = region:maghreb_region
			}
		}
	}

	unlock_unit = n_ens_late_xebec


	ai_preference_tags = {
		exploration
	}

	ai_weight = {
		add = "ai_unlock_unit_score(n_ens_late_xebec)"
	}
}